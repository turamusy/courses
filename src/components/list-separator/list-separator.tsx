import { View } from "react-native";
import { styles } from "./list-separator.style";

/** A customized element for separating list items */
const ItemSeparator = () => <View style={styles.itemSeparator} />;

export default ItemSeparator;