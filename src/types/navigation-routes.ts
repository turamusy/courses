/**
 * @description {object} NAVIGATION - An object containing screen names that will be imported every-time we use navigation.navigate().
 */
export enum NAVIGATION {
  HOME = 'Home',
  TOPIC_SELECTOR = 'TopicSelector',
}
